@using Microsoft.AspNet.Identity
<div class="header-middle">
    <!--header-middle-->
    <div class="container">
        <div class="row">
            <div class="col-sm-4">
                <div class="logo pull-left">
                    <a href="@Url.Action("Index","Home")"><img src="~/Content/Website-Images/logo.png" alt="" /> </a>
                </div>
                @*<div class="btn-group pull-right">
                    <div class="btn-group">
                        <button type="button" class="btn btn-default dropdown-toggle usa" data-toggle="dropdown">
                            USA
                            <span class="caret"></span>
                        </button>
                        <ul class="dropdown-menu">
                            <li><a href="#">Canada</a></li>
                            <li><a href="#">UK</a></li>
                        </ul>
                    </div>

                    <div class="btn-group">
                        <button type="button" class="btn btn-default dropdown-toggle usa" data-toggle="dropdown">
                            DOLLAR
                            <span class="caret"></span>
                        </button>
                        <ul class="dropdown-menu">
                            <li><a href="#">Canadian Dollar</a></li>
                            <li><a href="#">Pound</a></li>
                        </ul>
                    </div>
                </div>*@
            </div>
            <div class="col-sm-8">
                <div class="shop-menu pull-right">
                    <ul class="nav navbar-nav">
                        @*<li><a href="#"><i class="fa fa-star"></i> Wishlist</a></li>
                        <li><a href="checkout.html"><i class="fa fa-crosshairs"></i> Checkout</a></li>*@
                        @if (Request.IsAuthenticated)
                        {
                            <li><a href="@Url.Action("Index","Manage")"><i class="fa fa-user"></i> My Balance</a></li>
                            <li><a href="@Url.Action("GetShoppingCartItems", "ShoppingCarts")"><i class="fa fa-shopping-cart"></i>
                                <span id="itemsInCart" style="color:tomato">0</span> Item(s) in Cart</a></li>
                            <li><a href=@Url.Action("Index", "Manage")><i class="fa fa-envelope"></i><span style="color:tomato" id="userMessages">0</span> <span>Hello </span>@User.Identity.Name</a></li>
                            <li><a href=@Url.Action("LogOff","Account")>Log off</a></li>
                        }
                        else
                        {
                            <li><a href=@Url.Action("Register", "Account")><i class="fa fa-lock"></i> Register</a></li>
                            <li><a href=@Url.Action("Login", "Account")><i class="fa fa-lock"></i> Login</a></li>
                        }
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="header-bottom">
    <!--header-bottom-->
    <div class="container">
        <div class="row">
            <div class="col-sm-12">
                @using (Html.BeginForm("Search", "Products", FormMethod.Get, new { id = "searchForm" }))
                {
                    <div class="search_box centered">
                        <input type="text" name="searchText" value="@(ViewBag.SearchTerm ?? "")" placeholder="Search">
                        @*<input type="hidden" id="searchPageNo" name="searchPage" value="1" />*@
                        @*<input type="hidden" name="PageStatus" value="@(ViewBag.PageStatus ?? "1")" />*@
                    </div>
                }
            </div>
        </div>
    </div>
</div>

